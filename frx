def count_strings(lst):
        count=0
        for string in lst:
                if len(string)>=2 and string[0]==string[-1]:
                        count+=1
        return count
strings=["aba","xyz","a","abc","aaa","xy"]
result=count_strings(strings)
print("Number of matching strings:",result)
